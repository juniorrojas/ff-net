{
  "scripts": {
    "build": "export NODEPATH=.. && rollup -c rollup.config.js",
    "watch": "export NODEPATH=.. && rollup -c rollup.config.js --watch",
    "test": "export NODE_PATH=. && jest"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^24.1.0",
    "@rollup/plugin-node-resolve": "^15.0.2",
    "jest": "^29.5.0",
    "puppeteer": "^19.7.4",
    "rollup": "^2.79.1",
    "rollup-plugin-terser": "^7.0.2"
  }
}
