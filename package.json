{
  "name": "ff-net",
  "author": {
    "name": "Junior Rojas"
  },
  "scripts": {
    "watch": "mkdir -p js.build.out && watchify js/main.js -o js.build.out/main.js",
    "build": "mkdir -p js.build.out && browserify js/main.js -o js.build.out/main.js"
  },
  "devDependencies": {
    "browserify": "^16.5.0",
    "glob-parent": ">=5.1.2",
    "watchify": "^4.0.0"
  }
}
